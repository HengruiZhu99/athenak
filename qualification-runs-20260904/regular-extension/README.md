# Regular extension evidence, 2026-09-04

Classification: partial formulation/implementation improvement; CUDA evolution
qualification pending. Read `docs/pc_gh_regular_extension.md` in the source tree.

Authoritative latest checks:
- `complete-final-audit/commands.json` and its seven logs: exact current symbolic
  proofs, full production principal comparison, exact all-k Fourier polynomial.
- `production-final-zero-step/`: final compiled principal/source matrices and an
  independent exactly reduction-satisfying point jet, with command and run log.
- `full-symbolic.log`: existing independent first-principles regression suite.
- `pulse-initial-oracles/{p,Q,L,B}`: zero-step component/initializer checks.
- `pulse-initial-oracles/Q-amr-3d`: three-dimensional hierarchy/initialization
  check only; no RK evolution took place.
- `inputs-v2`: current explicit CUDA input collection, generated by make_inputs.py.
- `build-final.log`, `configure.log`, `CMakeCache.txt`, `provenance.json`, and
  `artifact-sha256.json`: local build configuration and source/evidence identity.
- `ssh-access-final.log`: remote authentication still denied; no CUDA run started.

Earlier records are retained deliberately. `coupled-prototype` is a superseded
formulation. `production-symbol-coupled.log` records a failed curl-B comparison
that prompted the implemented completion. `symbol-coordinate.log` used a pivoted
inverse with artificial poles; the final polynomial shear avoids those poles.
`wormhole-subsidiary.log` failed a structural SymPy equality assertion; the final
expanded residual assertion passes. `pulse-initial-oracles/Q-amr` is the failed
one-dimensional SMR initialization fixture; the debugger did not produce a
backtrace and was terminated. The three-dimensional fixture passed. `inputs` is
the earlier fixture collection and should not be used for AMR qualification.

All local executable runs here are at cycle=0, t=0. No single-puncture evolution,
AMR pulse evolution, or binary merger has been run for this candidate. Preserve
the old 20260902 evidence and original e7aca617 FO-GH rejection audit separately.
