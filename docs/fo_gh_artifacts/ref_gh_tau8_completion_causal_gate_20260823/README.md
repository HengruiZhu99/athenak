# Ref-GH tau-8 completion causal-gate artifacts

This directory contains compact evidence only.  Large restart and field-output
files remain on the execution filesystem and must not be committed.

Current local evidence:

- `local/phase0/`: compiled gate and standalone audit logs;
- `local/diagnostic_smoke/`: post-dedup history/max-location smoke and parsed
  causal-analysis output;
- `local/authoritative_mesh_tree.{json,tsv}`: exact 272-block enlarged tree;
- `local/existing_outer_feature.{json,tsv}`: old-domain retained-shell onset
  extraction and explicit unresolved classification.

Aurora preflight and evolution artifacts will be added under `aurora/` only
after renewed interactive authentication, successful jobs, and compact
collection.  The report `docs/ref_gh_tau8_completion_causal_gate_20260823.md`
distinguishes completed evidence from pending gates.
