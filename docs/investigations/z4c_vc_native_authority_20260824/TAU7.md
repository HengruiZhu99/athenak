# Tau approximately 7 milestone

The N256 native record reaches tau=6.84721 but fails before tau approximately
7. The first accepted late refinement follows substantial fixed-hierarchy
constraint growth, and the subsequent hierarchy cascades to logical level 23
while coordinate time stalls near `t=11.19288795 M`.

Disposition: `NATIVE_AMR_UNSTABLE`.

Per the staged gate, no N128/N512 replay was run for this failed authority and
the authority must not be used for production qualification.
