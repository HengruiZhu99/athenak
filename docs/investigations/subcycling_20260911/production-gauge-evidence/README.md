Production telegraph parameters tau=kappa=.01, spatial_order4/extrap_order2, smooth gauge pulse. Live/frozen/restart checks pass at dt=.0004. Fixed-time refinement dt=.002,.001,.0005 duration=.016 passes with ratios16.07988 and16.01309. Smaller .0004/.0002/.0001 test cannot resolve order (RMS differences1e-16); do not interpret it as a successful order test. Larger dt=.004 has hierarchy/native synchronous difference4.968859e-12 (component23), failing the unchanged1e-12 gate; cause remains to investigate. Original probe created a2.6e-18 leftover endpoint step rejected by native central diagnostics; probe now absorbs a few-ulp remainder into last nominal cap, without bypassing spatial/source limits. This removed that failure. These are not production strong-field results.
