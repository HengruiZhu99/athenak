Synchronous stage gauge fix

The hierarchy owner previously queried the physical dense-output maximum at nominal stage times even for a group containing every level. RK stages2 and3 differ despite sharing time. When all levels share a stage, reduce its actual physical-leaf values instead, and apply the same maximum to the stage stability check. Asynchronous groups retain the common-time predictor. Production tau=kappa=.01, spatial4/extrap2 comparison at dt=.004 now differs from native synchronous by6.78e-16 instead of4.97e-12. Fixed-duration temporal ratios16.1261(sync),16.0903(subcycled). Live/frozen and restart tests pass; real refine/coarsen and mixed-event restarts pass. Stage-limit failure/retry test passes. GPU confirmation and Brill reproduction remain outstanding.
