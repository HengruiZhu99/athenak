# KO=0.5 Rout=128 authority campaign

Status: in progress.

The numerical configuration is the previously qualified native-VC Rout=128
SMR setup with only `diss=0.50` and the coordinate-time target changed to
`t=30M`. N256 records a fresh accepted AMR hierarchy; N128 and N512 replay
that exact physical-time schedule. Runtime provenance and terminal evidence
will be added after each immutable segment is verified.
