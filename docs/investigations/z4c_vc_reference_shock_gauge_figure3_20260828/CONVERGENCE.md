# Convergence status

## Disposition

`NOT_RUN_BECAUSE_N256_SCIENTIFIC_GATE_FAILED`

The controlling workflow allowed N128 and N512 same-hierarchy replay only if
the fresh N256 reference run passed the Figure-3 reproduction gate. N256
reached its coordinate-time limit, but its apparent first curvature peak is
constraint invalid and it did not reach the published deep-minimum/rebound
region. The replay campaign was therefore not executed.

There is no three-resolution comparison, constraint convergence measurement,
or observed convergence order for this campaign. Nothing in this bundle
supports a convergence or full Figure-3 reproduction claim.
