# Convergence status

## Disposition

`NOT_REACHED`

The controlling plan permits N128 and N512 AMR replay only after the fresh N256
run passes the Figure-3 reproduction gate. N256 was not executed, so no replay
authority was recorded and neither N128 nor N512 was run.

There is therefore no three-resolution comparison, constraint convergence
measurement, or convergence order for this campaign. No convergence or
Figure-3 qualification claim is supported.
