# N1024 CFL 0.40 / KO 0.10 exact-tree ablation

## Disposition

`EXACT_REPLAY_VERIFIED; FAILED_BEFORE_FIRST_PEAK; TWO_PARAMETER_CAUSALITY_UNRESOLVED`

This N1024 run reused the baseline physical problem, cells per MeshBlock,
four-A100 rank layout, and exact N256 AMR authority. It changed only two
runtime controls together:

- CFL number: `0.15 -> 0.40`;
- KO dissipation: `0.50 -> 0.10`.

Both authority events were accepted at their exact physical times, with the
same leaf counts and tree checksums as the baseline. The last history row is
at coordinate time `16.717628536243680 M`, central proper time
`8.687343281197583`, cycle 4916. The strict post-RK gate stopped the evolution
at coordinate time `16.72465031794259 M`, cycle 4919, RK stage 1.

## Failure state

The gate reported `nonpositive_metric_pivot_1` at
`(rho,z)=(0.0625,19.96875)`, MeshBlock GID 141, level 6. Chi remained positive
at `0.9166010786594372`, while

```text
det(gtilde) = -0.2918387815330118
SPD pivots  = [18.87497629550635, -0.2307199370145590,
               -0.2918387815330118]
Khat        = 161.9245594105016
Theta       = 155.6952328224990
```

This is not a nonpositive-chi parent-stencil failure. It is an evolved-state
loss of conformal-metric positive definiteness after a large constraint
runaway.

## Constraint trajectory

Baseline and ablation initial constraint integrals are identical. Their
initial C squared integral is `1.2881308780515045e-08`. The ablation C squared
integral first crosses:

| threshold | central proper time |
|---:|---:|
| `0.01` | 7.54619 |
| `0.1` | 7.69547 |
| `1` | 7.85416 |
| `10` | 8.04005 |
| `100` | 8.24578 |
| `1000` | 8.46869 |
| `10000` | 8.65239 |

The final history value is `4.9679e4`, corresponding to C amplitude `222.89`;
the completed baseline maximum amplitude is `0.197`. The ablation therefore
does not cure the residual constraint issue and fails before the published
first curvature peak.

## Interpretation boundary

Supported: the combined CFL 0.40 / KO 0.10 setting is unstable for this exact
N1024 replay and produces a catastrophic runaway absent from baseline.

Not supported: attribution to CFL alone, KO alone, the continuum formulation,
or a unique source defect. Isolating time-step versus dissipation sensitivity
would require one-knob controls, which were not requested or run here.

Job `57706639` contains the failed partial trajectory. Job `57706621` failed
before science initialization because the first runner used the invalid key
`time/cfl`; it is preserved as a startup-only provenance record.
