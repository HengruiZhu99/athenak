# Figure-3 authority and production binding

This campaign binds to Figure 3 of `arXiv:2607.10843v1`. It does not
reconstruct the figure from memory.

## Published authority

- Family: time-symmetric off-center Brill data from Eq. (3).
- Parameters: `A=-0.047`, `rho0=5`, `z0=0`, `sigma_rho=sigma_z=1`.
- Quantity: `log10(abs(I))`, where `I=R_abcd R^abcd`, sampled at the spacetime
  origin.
- Horizontal coordinate: proper time `tau` accumulated at the origin.
- Normalization: the paper plots the raw width-normalized code units fixed by
  `sigma_rho=sigma_z=1`; neither `tau` nor `I` is divided or multiplied by the
  ADM mass. The ADM mass is reported separately.
- Published source figure SHA-256:
  `bbbfdd618909eef7d4d9597859590457f310b3d790aef0ca51252c60787375d5`.
- Authenticated rendered-curve CSV:
  `../brill_o4_dchi001_replay_convergence_20260821/evidence/reference/figure3_published_curves.csv`.
- Authenticated curve metadata:
  `../brill_o4_dchi001_replay_convergence_20260821/evidence/reference/figure3_published_curves_metadata.json`.
- Existing plotting authority:
  `../brill_o4_dchi001_replay_convergence_20260821/scripts/plot_common_tree_figure3.py`.

The rendered paper curves are not embedded original numerical data; they are
vector-path centerlines extracted from the frozen PDF. Their uncertainty and
independent raster check are recorded in the metadata.

## Initial data

The production coefficient file is:

`../z4c_vc_cartoon_axis_boundary_20260823/evidence/brill_global_128x32_origin_control.coefficients`

Its immutable properties are:

- SHA-256: `1b5f0efc3f080215ed7d7994194ba63ea123415bfd8e74c54ca1fd72680aea10`;
- dimensions: `radial_points=128`, `angular_points=32`;
- ADM mass: `2.6606354586228815`;
- exporter Hamiltonian residual: `2.8081071254899221e-12`;
- origin behavior: non-spherical modes are at roundoff in the existing
  coefficient-resolution audit.

The file was generated by an existing dirty/uncommitted IrisK exporter
worktree. Exact bytes are preserved and adequate for this AthenaK numerical
control, but this is not an upstream IrisK-exporter qualification.

## AthenaK production configuration

The AthenaK evolution is a gauge analogue of the published three-code result,
not a code-by-code gauge identity. The fixed production choices are native
vertex centering, Cartoon SO(2), O4 finite differences, q6 vertex transfer,
quadratic (`extrap_order=3`) physical ghosts, RK4, CFL 0.15, KO 0.02,
max-domain-|K|-scaled telegraph lapse with `tau=kappa=1`, Gamma-driver shift
with `eta=2`, no Z4 damping, no chi floor, and pre-collapsed `psi^-2` lapse.

The physical domain is `rho in [0,16]`, `z in [-16,16]`, with a fixed 4-by-8
root-MeshBlock lattice. N128/N256/N512 use 16/32/64 active cells per physical
MeshBlock. N256 records the AMR hierarchy and N128/N512 replay its accepted
leaf tree at identical physical event times.

