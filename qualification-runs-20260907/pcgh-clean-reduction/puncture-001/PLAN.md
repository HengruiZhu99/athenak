# Puncture campaign: user-directed continuation

The user explicitly stopped interface diagnosis and requested uniform and SMR puncture stability/convergence, followed by binary evolution. This overrides the earlier interface promotion ordering, not the evidence standards. No interface qualification is claimed.

Interface issues frozen at c8b81f42: ordinary and residual-repaired smooth SMR evolution both failed signed field-difference alignment; residual repair increased fine-grid curl error. Six-point 2D restriction reduced some errors but did not qualify transfer. The private-buffer trace identifies a discrete derivative/transfer commutator and its contribution to the immediate C RHS; it does not establish a cause of late instability. See INTRINSIC_INTERFACE_BUDGET.md, INTRINSIC_SMR_EVOLUTION.md, INTRINSIC_SMR_STAGE_BUDGET.md and INTRINSIC_P_COMMUTATOR.md. An unfinished six-point prolongation control passed a polynomial unit test only, never a mesh evolution; its patch and source are preserved outside Git at /Users/hz0693/research/pcgh-clean-reduction-tests-20260907/intrinsic-point-prolong-001/unfinished. It is not enabled in this campaign.

Initial single-hole data: time-symmetric M=1 Schwarzschild wormhole, w=(r/(r+M/2))^2, rho=1, p=l=M*x/(r+M/2)^3, all other intrinsic components zero. Cell-centered grids avoid the puncture point. Direct lapse/Gamma-like gauge, FD6, RK3, CFL 0.2, KO 0.3, eta=2, kappa=1, lambda=alpha/M, no GH/auxiliary projection, no floor, ordinary transfer. Outflow uses existing component-wise linear ghost extrapolation and second-order radial Sommerfeld RHS with the 50-field flat state; boundaries are not constraint-preserving.

Frozen adaptive test sequence: first CPU short 3D uniform/SMR smoke (n=16, T=.02), then CUDA CPU comparison and three resolutions. Uniform domain [-8,8]^3, n=32/64/128, h=.5/.25/.125. SMR same domain, root=32/64/128 with one fixed refinement box [-4,4]^3, finest h=.25/.125/.0625; block width=root/4 (keeps interfaces at +/-4M at every resolution). First common target T=2, temporal control halves the timestep at the middle resolution. Continue health screens to T=20 where finite admissible evolution permits; preserve first bad state and last checkpoints on failure. These coarse levels are a first convergence test, not sufficient resolved-core qualification. Domain +/-8 allows early interior comparison; late boundary-contaminated data cannot qualify a physical solution. After this screen, proceed to larger-domain SMR and the binary requested by the user, preserving any failed prerequisites as limitations.

Compare signed primary fields on common points and fixed annuli [.5,1),[1,2),[2,4), plus full domain; use two interpolation orders as an error control. Report difference ratios and cosine alignment; positive norm order with poor alignment is a failure. Require alignment >=.95 for a provisional asymptotic claim and timestep-halving differences <10% of spatial differences. No fixed high-order claim at the puncture. Record all existing 89 full-domain physical/reduction/curl components and stage health, and derive regional diagnostics from saved double-precision states. Stop on nonfinite/nonpositive state or excluded hyperbolicity domain; no clipping or constraint reset. Survival alone never passes convergence.

Resources: authorized direct della-vis1 A100 40GB, observed free 36283 MiB before build. One run at a time, planned maximum initial active cells 128^3, no unrelated processes touched. Start with short screens and measure runtime before extending. Preserve executable/source/input hashes, controller exit status and checkpoints; do not relaunch on an observation timeout.

Setup note: the four-cell-block SMR smoke produced zero unused corner ghost states and failed validation before evolution. It is retained. The campaign uses eight-cell or larger blocks (root 32/64/128), which passed initialization and the two-step smoke at root 32. No interface repair was attempted. An input-generation typo was also rejected before initialization and retained.
