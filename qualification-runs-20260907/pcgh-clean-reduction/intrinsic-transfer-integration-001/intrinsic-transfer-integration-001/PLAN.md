Frozen smoke gates: serial FD2/4/6 2D/3D static periodic topology, nlim=0, matched none/residual_shifted. Require success, identical mesh maps, exact preservation of all stored first20 and all active50. Record auxiliary ghost changes, not an accuracy claim. One step FD6 2D then checks finite state only, not convergence. Uniform default must match previous binary bitwise; existing restart controls must pass. No black-hole promotion.
